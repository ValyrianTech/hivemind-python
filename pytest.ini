[pytest]
testpaths = tests
python_files = test_*.py
addopts = --cov=hivemind --cov-report=term-missing --cov-report=html
markers =
    unit: mark test as a unit test
    integration: mark test as an integration test
